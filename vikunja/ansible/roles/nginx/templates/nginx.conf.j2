server {
    listen       80;
    # server_name  localhost;
    server_name _;

    location / {
        root   /app/vikunja;
        try_files $uri $uri/ /;
        index  index.html index.htm;
    }
}